# Todo

## Versioning

How should the multi-language wrapper handle versioning?

## Markdown for llms.txt 

Which generators can emit markdown for the 
[llms.txt pipeline](./discoverability.md)? 
Can we develop workarounds for those that lack this support?

Relatedly, how can we link these llms.txt files from the autogenerated
signalwire.com/docs/llms.txt 
index?

We also need to auto serve the md version if
fetch content type is set as markdown
